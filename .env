#!/bin/bash
PROJECT_ID=$(gcloud config list project --format "value(core.project)")
IMAGE_REPO_NAME=node2vec-classification
IMAGE_TAG=latest

export IMAGE_URI=gcr.io/$PROJECT_ID/$IMAGE_REPO_NAME:$IMAGE_TAG
